from {{ project_name }}.pipeline.processing import Processing
from {{ project_name }}.pipeline.train import Train
from {{ project_name }}.pipeline.predict import Predict


def test_processing():
    assert 'something'


def test_train():
    assert 'something'


def test_predict():
    assert 'something'
